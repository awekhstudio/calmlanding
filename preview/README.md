# Preview assets

These files are the current repository cover and responsive screenshots. Replace them only when the released interface changes.

- `calmlanding-cover.jpg`: 1600 × 900 px
- `calmlanding-desktop.jpg`: at least 1440 px wide
- `calmlanding-mobile.jpg`: capture at a 390 px viewport, optionally placed on a wider canvas

Keep captures free of browser developer tools, cursors, selection overlays, and extension UI. The interactive product compositions used inside the page live separately in `assets/images/`.
